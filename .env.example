DJANGO_SETTINGS=prod_or_dev
SECRET_KEY

EMAIL_HOST
EMAIL_PORT
EMAIL_HOST_USER
EMAIL_HOST_PASSWORD
DEFAULT_FROM_EMAIL

CELERY_BROKER_URL
CELERY_RESULT_BACKEND

CHANNEL_REDIS_URL
CACHE_REDIS_URL

# Master key for envelope-encrypted file storage + field-level PII encryption.
# Base64(URL-safe) of 32 random bytes.
FILE_ENCRYPTION_KEY

# Optional: separate signing key for short-lived download URLs (default = SECRET_KEY).
# DOWNLOAD_TOKEN_SIGNING_KEY
# Default 600 seconds (10 min).
DOWNLOAD_TOKEN_TTL_SECONDS
